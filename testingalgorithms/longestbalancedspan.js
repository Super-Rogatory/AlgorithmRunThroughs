function longest_balanced_span(V){
    let best = [];
    let sum_elem_in_V = 0;
    for(let s = 0; s < V.length; s++) {
        sum_elem_in_V += V[s];
        for(let e = s + 1; e < V.length; e++) {
            if(sum_elem_in_V === 0){
                if(best.length === 0 ||)
            }
        }
    }
}